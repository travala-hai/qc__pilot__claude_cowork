# TC-017: Check display of search results when updating search location

**URL(s) under test:** https://www.travala.com/ → https://www.travala.com/search
**Priority:** (not specified in source)

**Preconditions:**
- User is on the search results page (`/search`), having already completed an initial search.

**Failure handling (default):** Stop and report on any failed CHECKPOINT, unless noted otherwise.

> **Source note:** the raw test steps were a single BDD-style cell ("Given navigate to Travala site → Search → Change location → Click Search again → Verify results") with no test data and no concrete expected result beyond "Search results update per new location." This required substantial decomposition; see the flag in step 5.

## Steps

1. **[ACTION]** Navigate to `https://www.travala.com/` if not already there.
2. **[ACTION]** Perform an initial search: select a destination, select check-in/check-out dates, and click SEARCH (i.e., the flow covered by TC-006/TC-007/TC-009/TC-010), to reach the search results page. **[ASSUMED — reason: the raw step "Search" is a single word with no detail; expanded into the concrete actions used elsewhere in this suite, per rule 1's instruction to split compound/vague instructions into atomic steps.]**
3. **[CHECKPOINT]** Confirm the search results page has loaded with a result count greater than 0 for the initial destination. Fail if results did not load. **[ASSUMED — reason: needed as a verified starting state before changing location, per "Known starting state" checklist item.]**
4. **[ACTION]** Locate the destination/location field or "Change search"/"Modify search" control on the results page; if not present after a reasonable wait, fail this step. **[ASSUMED — reason: "Element existence" checklist item.]**
5. **[FLAGGED — cannot be fully inferred]** The source does not specify **which** new destination to change to, nor precisely what "results update per new location" should look like (e.g., does it mean only the result set changes, or also that the on-page destination label / URL updates?). This is needed before the test can be run deterministically. Recommend the test author specify a concrete target destination (e.g., "change to Bangkok, Thailand") and confirm the intended assertion scope. The steps below proceed with a placeholder target destination and assert the most literal reading of "results update."
6. **[ACTION]** Change the location field to a different destination than the initial search (placeholder: a different city, e.g., Bangkok, Thailand) and select it from the autocomplete dropdown. **[ASSUMED placeholder target — see flag in step 5; confirm the actual target destination before running.]**
7. **[ACTION]** Click the SEARCH button again.
8. **[ACTION]** Wait for the results page to reload. Condition: the URL's location parameter changes AND the loading spinner/skeleton clears. Fallback: wait up to 6 seconds (consistent with TC-010's benchmark). **[ASSUMED — reason: "Post-action wait" checklist item.]**
9. **[CHECKPOINT]** Confirm the URL's location parameter has changed to match the newly selected destination. Fail if it still reflects the original destination.
10. **[CHECKPOINT]** Confirm the displayed destination label on the results page (e.g., page heading or breadcrumb) matches the newly selected destination. Fail if it still shows the original destination.
11. **[CHECKPOINT]** Confirm the result count and/or hotel cards shown have changed from the initial search's results (i.e., are not an identical, unchanged result set). Fail if the results are identical to the pre-change results.
