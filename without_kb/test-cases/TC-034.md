# TC-034: Check display of packages when applying a room-type filter

**URL(s) under test:** Hotel property/detail page (specific hotel not specified — see step 1)
**Priority:** (not specified in source)

**Preconditions:**
- User is on a hotel property/detail page.

**Failure handling (default):** Stop and report on any failed CHECKPOINT, unless noted otherwise.

> **Source note:** the raw test steps were a single BDD-style cell ("Given open Property → Select RoomTypes → Apply Twin filter → Verify package") with no test data and a vague expected result ("Package filtered successfully"). This required substantial decomposition; see flags below.

## Steps

1. **[ACTION]** Ensure a hotel property/detail page is loaded — either continuing from a prior detail-page context in this suite (e.g., TC-023's InterContinental page), or by navigating: search → open any hotel card. **[FLAGGED — reason: the source's "open Property" doesn't specify which hotel; reusing prior suite context where available, since the stated precondition ("User is on Property Page") doesn't require a specific property.]**
2. **[CHECKPOINT]** Confirm the property/detail page has finished loading and a "Room Types" section/tab and its packages are visible. Fail if not present after a reasonable wait. **[ASSUMED — reason: "Element existence" + "Post-navigation wait" checklist items.]**
3. **[ACTION]** Locate and click into the "Room Types" section/tab. **[FLAGGED — reason: the source's "Select RoomTypes" doesn't clarify whether this means clicking a tab, a dropdown, or scrolling to a section; the most literal reading (a selectable control) is used here but should be confirmed against the actual UI.]**
4. **[ACTION]** Locate the room-type/package filter control on the page (e.g., a filter bar or dropdown for bed/room configuration); if not present after a reasonable wait, fail this step. **[ASSUMED — reason: "Element existence" checklist item.]**
5. **[ACTION]** Record the full, unfiltered list of packages/room types shown before applying any filter, including each package's room-type label. **[ASSUMED — reason: needed as a "before" baseline so the filtered result can be checked concretely, per rule 2.]**
6. **[ACTION]** Apply the "Twin" filter (select the filter option labeled "Twin" or an equivalent, e.g., "Twin Room" / "Twin Beds").
7. **[ACTION]** Wait for the package list to update. Condition: the package list content changes from the pre-filter list recorded in step 5, or a loading indicator clears. Fallback: wait up to 5 seconds. **[ASSUMED — reason: "Post-action wait" checklist item.]**
8. **[CHECKPOINT]** Confirm every package now displayed has a room-type label containing "Twin" (e.g., "Twin Room", "Twin Beds"). Fail if any displayed package doesn't reference a twin configuration. **[This is the concrete definition of "filtered successfully" per rule 2 — the source doesn't specify what a correctly filtered result looks like.]**
9. **[CHECKPOINT]** Confirm at least one package remains displayed after filtering, OR — if zero Twin packages exist for this property — confirm an explicit "no results" message is shown rather than a blank page. Fail if the page is blank with no messaging. **[ASSUMED — reason: edge-case coverage implied by "filtered successfully"; a silent empty state would itself be a bug.]**
10. **[CHECKPOINT]** Confirm no non-Twin packages from the original unfiltered list (step 5) remain visible after filtering. Fail if any are still shown.
