# TC-010: Verify search submit navigates to results page with correct URL parameters

**URL(s) under test:** https://www.travala.com/ → https://www.travala.com/search
**Priority:** (not specified in source)

**Preconditions:**
- Destination set to "Hanoi, Vietnam"; dates 13–14 Jun 2026 selected; occupancy 1 room / 2 adults (i.e., continuing from TC-009's end state).

**Failure handling (default):** Stop and report on any failed CHECKPOINT, unless noted otherwise.

## Steps

1. **[ACTION]** If not continuing directly from TC-009 in this suite, perform setup: complete destination selection (Hanoi, Vietnam) and date selection (13–14 Jun 2026) as in TC-007/TC-009. **[ASSUMED — reason: standalone runnability.]**
2. **[ACTION]** Confirm the rooms/occupancy picker is visible (it should have auto-opened after date selection per TC-009); if not visible, click the guests/rooms field to open it. **[ASSUMED — reason: "Element existence" checklist item.]**
3. **[ACTION]** Confirm occupancy is set to exactly 1 room, 2 adults, 0 children; if it differs, adjust the counters until it matches. **[ASSUMED — reason: "Field hygiene / known starting state" checklist item — the precondition requires this exact configuration, but the source doesn't state how to reach it if the default differs.]**
4. **[ACTION]** Close the rooms/occupancy picker (e.g., click outside it or use its confirm/apply control) if it must be closed before the SEARCH button becomes clickable. **[ASSUMED — reason: "Ambient obstacles" checklist item — the open picker could overlay the search button.]**
5. **[ACTION]** Confirm the SEARCH button is visible and enabled; if not, after a reasonable wait, fail this step. **[ASSUMED — reason: "Element existence" checklist item.]**
6. **[ACTION]** Click the SEARCH button.
7. **[ACTION]** Wait for the results page to load. Condition: URL path changes to `/search` AND the results-loading spinner/skeleton has cleared. Fallback: wait up to 6 seconds (per the source's own "~6s" benchmark) if no discrete signal is detectable. **[ASSUMED — reason: "Post-navigation wait" checklist item, tied to the source's stated ~6s expectation.]**
8. **[CHECKPOINT]** Confirm the URL path is `/search`. Fail otherwise.
9. **[CHECKPOINT]** Confirm the URL query parameters reflect the search criteria: a check-in parameter equal to `2026-06-13`, a check-out parameter equal to `2026-06-14`, and a location/destination parameter corresponding to Hanoi, Vietnam. **[ASSUMED concretization — reason: the source's Expected Result only says "all criteria reflected in URL params" without naming them; parameter names/format are inferred from this same test's own historical Actual Result (`check_in=2026-06-13&check_out=2026-06-14&location=6054458`), per rule 2's instruction to make vague expectations concrete.]**
10. **[CHECKPOINT]** Confirm the results page displays a result count greater than 0 (e.g., an "X properties found" label with X > 0). Fail if the count is 0 or the label is absent.
11. **[CHECKPOINT]** Confirm at least one hotel result card is rendered on the page within 6 seconds of clicking SEARCH. Fail if no cards render within that window.
